# New Project Ideas — February 16, 2026 (6:03 AM)

## Current Project Inventory

### Built (14 projects)
1. **clipkeep** — Clipboard manager with auto-categorization
2. **devsnap** — Dev environment backup/restore
3. **file-organizer** — Content-aware file organization
4. **filesage** — Natural language file search (embeddings)
5. **git-time-machine** — Git history visualization
6. **local-search** — Fast full-text file search
7. **slidedeck** — Markdown to HTML presentations
8. **smart-memory** — Semantic memory for AI assistants
9. **task-cli** — Task management with natural language dates
10. **tidyspace** — Workspace cleanup tool
11. **webhook-inspector** — Self-hosted webhook debugger
12. **quickserve** — Instant HTTP file sharing server
13. **system-pulse** — System monitor with history
14. **booking-system** — (appears to be a newer addition)

### In Active Development
1. **promo-scanner** — Screenshot-based promotion detection for bookmakers
2. **notify-router** — Smart notification routing system
3. **surebet-detector** — Arbitrage/+EV detection across sportsbooks

### Gaps Identified
- No API testing/development tools
- No data visualization tools
- No code quality/analysis tools
- No backup/sync utilities
- No time tracking tools
- No note-taking/knowledge tools
- No image/media processing tools
- No network diagnostic tools

---

## New Project Ideas

### 1. API Workbench — HTTP Client for Terminal ⭐ SELECTED
**Problem**: `curl` is powerful but painful; Postman is heavy and GUI-only.
**Solution**: A terminal HTTP client with:
- Request collections organized by project
- Environment variables (dev/staging/prod)
- Response syntax highlighting (JSON, XML, HTML)
- Request history and replay
- Export to curl/command
- Chain requests (use response data in next request)

**Tech**: Node.js (pure, zero dependencies)
**Value**: Fast API testing without leaving terminal

**Why selected:**
- Fills a real gap — no HTTP client exists in the toolkit
- Complements webhook-inspector nicely
- Useful for testing notify-router, surebet-detector APIs
- Pure Node.js means instant startup, no compile step
- Can use it immediately for the betting tools' APIs

---

### 2. TimeTracker — Simple Time Logging CLI
**Problem**: Time tracking apps are bloated; forgetting what you worked on.
**Solution**: Dead-simple time tracking:
- `tt start "fixing bug #123"` — Start tracking
- `tt stop` — Stop and log
- `tt status` — Current activity + duration
- `tt report --today` — Summary of today's work
- `tt report --week` — Weekly breakdown by project/tag
- Export to CSV/JSON for invoicing

**Tech**: Node.js + SQLite
**Value**: Know where your time goes, no friction

---

### 3. CodeStats — Codebase Analytics Tool
**Problem**: Understanding a codebase's health requires multiple tools.
**Solution**: A code analyzer that shows:
- Lines of code by language
- Complexity metrics (cyclomatic, cognitive)
- Dependency graph visualization
- TODO/FIXME comment tracker
- Test coverage summary
- Churn analysis (files changed most often)

**Tech**: Node.js + tree-sitter parsers
**Value**: Quick codebase health check

---

### 4. SnapSync — Directory Snapshot & Sync
**Problem**: Need to backup/sync directories without full git repos.
**Solution**: Snapshot-based backup:
- `snapsync snap ~/important` — Create snapshot
- `snapsync list` — Show all snapshots
- `snapsync diff snap1 snap2` — Compare snapshots
- `snapsync restore snap1` — Restore from snapshot
- `snapsync sync local remote` — Sync to another location
- Deduplication (hard links for unchanged files)

**Tech**: Node.js + native fs operations
**Value**: Fast, space-efficient backups

---

### 5. NoteStream — Append-Only Notes CLI
**Problem**: Notes apps are too structured; sometimes you just need to write.
**Solution**: A frictionless note-taking tool:
- `note "Had an idea for the API"` — Quick append
- `note -f ideas.md "Another thought"` — Write to specific file
- `note --list` — Show recent notes
- `note --search "API"` — Search all notes
- `note --today` — Show today's notes
- Daily files auto-organized by date

**Tech**: Node.js + Markdown files
**Value**: Capture thoughts instantly, find them later

---

### 6. NetCheck — Network Diagnostics Tool
**Problem**: `ping`, `traceroute`, `dig` are separate tools with different syntax.
**Solution**: Unified network diagnostics:
- `netcheck ping google.com` — With latency stats
- `netcheck dns google.com` — DNS resolution check
- `netcheck port localhost:3000` — Port connectivity
- `netcheck http api.example.com` — HTTP health check
- `netcheck trace google.com` — Traceroute with timing
- `netcheck speed` — Quick speed test

**Tech**: Node.js (net, dns, http modules)
**Value**: One tool for all network checks

---

### 7. DataViz — Terminal Data Visualizer
**Problem**: Looking at raw JSON/CSV is hard; opening Excel is slow.
**Solution**: Terminal data visualization:
- `dataviz stats data.json` — Summary statistics
- `dataviz hist data.csv --column age` — Histogram
- `dataviz plot data.csv --x time --y value` — ASCII line chart
- `dataviz table data.json` — Pretty table view
- `dataviz top data.csv --column sales` — Top N values

**Tech**: Node.js (pure)
**Value**: Understand data without leaving terminal

---

### 8. GitStandup — Daily Commit Reporter
**Problem**: Forgetting what you worked on yesterday; standup prep is tedious.
**Solution**: Git-based daily summary:
- `git-standup` — Show yesterday's commits across repos
- `git-standup --week` — This week's summary
- `git-standup --author "John"` — Filter by author
- `git-standup --since "2 days ago"` — Custom range
- Searches all git repos in a directory
- Formats nicely for standup meetings

**Tech**: Node.js + git CLI
**Value**: Automatic standup prep

---

### 9. ImageBox — Terminal Image Viewer/Converter
**Problem**: Can't quickly view or convert images in terminal/SSH.
**Solution**: Image operations in terminal:
- `imgbox view photo.jpg` — ASCII art preview
- `imgbox convert photo.jpg photo.png` — Format conversion
- `imgbox resize photo.jpg --width 800` — Resize
- `imgbox info photo.jpg` — Metadata display
- `imgbox thumbs *.jpg` — Generate thumbnails

**Tech**: Node.js + sharp (or pure with canvas)
**Value**: Quick image ops without GUI

---

### 10. EnvDiff — Environment Variable Diff Tool
**Problem**: Debugging "works on my machine" env issues.
**Solution**: Compare environment setups:
- `envdiff` — Show all env vars
- `envdiff --save prod` — Save current env as "prod"
- `envdiff prod dev` — Compare two saved envs
- `envdiff --check-required "DATABASE_URL,API_KEY"` — Validate
- `envdiff --template .env.example` — Check against template

**Tech**: Node.js (pure)
**Value**: Debug environment mismatches

---

## Selected for Building: API Workbench ⭐

**Project:** API Workbench — Terminal HTTP Client

**Features to build:**
1. Simple request execution: `apir GET https://api.example.com/users`
2. Method shortcuts: `apir POST /users -d '{"name":"John"}'`
3. Response formatting with syntax highlighting
4. Request history (last 100 requests)
5. Save/load request collections
6. Environment variables support
7. Chain requests with JSON path extraction

**Why this one:**
- Immediate utility for the existing betting tools (test their APIs)
- Complements webhook-inspector (request ↔ response tools)
- Pure Node.js = zero dependencies, instant start
- Different from existing tools (no HTTP client yet)
- Can use it right away to test notify-router endpoints

**Location:** `/root/.openclaw/workspace/projects/api-workbench/`

---

*Generated by Nemo 🐙 during cron job new-project-ideas*
